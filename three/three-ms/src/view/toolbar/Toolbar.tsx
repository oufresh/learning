import React from "react";
import style from "./Toolbar.module.css";
import cx from "classnames";

export const Toolbar = () => {
    return <aside className={cx("toolbar",style.Toolbar)}>
    </aside>;
}